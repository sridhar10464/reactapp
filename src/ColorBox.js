import * as React from "react";


export function ColorBox({ Color }) {
  const styles = {
    height: "25px",
    width: "250px",
    background: "color",
    marginTop: "10px",
  };
  return <div style={styles}></div>;
}
